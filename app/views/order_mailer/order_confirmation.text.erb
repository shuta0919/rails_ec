ご注文ありがとうございます

注文番号: <%= @order.id %>
注文日: <%= @order.created_at.strftime('%Y年%m月%d日') %>

注文詳細:
<% @order.order_items.each do |item| %>
商品名: <%= item.product.name %>
数量: <%= item.quantity %>
価格: <%= number_to_currency(item.total_price) %>
<% if @order.used_promotion_code %>
割引: <%= number_to_currency(@order.used_promotion_code.discount_amount) %>
合計: <%= number_to_currency(@order.total_price_with_discount) %>
<% else %>
合計: <%= number_to_currency(@order.total_price) %>
<% end %>
<% end %>

またのご利用をお待ちしております。